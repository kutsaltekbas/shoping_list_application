name: shoping_list_application
description: A new Flutter project.
publish_to: "none"
version: 0.1.0

environment:
  sdk: ">=3.1.3 <4.0.0"

dependencies:
  auto_route: ^7.8.4
  auto_route_generator: ^7.3.2
  build_runner: ^2.4.6
  firebase_core: ^2.19.0
  flutter:
    sdk: flutter
  flutter_screenutil: ^5.9.0
  json_annotation: ^4.8.1
  json_serializable: ^6.7.1
  provider: ^6.0.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
  assets:
    - asset/images/
